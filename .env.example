# Copy this to .env on the VM (same directory as docker-stack.yml) and fill in
# real values. `docker stack deploy` reads .env from the current directory for
# variable substitution. This file is gitignored — never commit real secrets.

POSTGRES_DB=mit_billing
POSTGRES_USER=mit_billing
POSTGRES_PASSWORD=changeme

OPENAI_API_KEY=
GEMINI_API_KEY=
